# 程序逻辑细则

本文档整理 `Daily-Word` 项目的主要设计逻辑、业务判断链路和工程约束。文档以当前代码为准，核心代码集中在：

- `scripts/daily_report_gui.py`：桌面 GUI 入口。
- `scripts/generate_daily_reports.py`：日报生成总调度入口。
- `scripts/convert_xls_to_xlsx.py`：`.xls` 转 `.xlsx`。
- `scripts/extract_xls_structure.py`：台账结构化抽取。
- `scripts/daily_report_builder.py`：街道日报业务数据构建。
- `scripts/daily_report_renderer.py`：街道日报 Word 渲染。
- `scripts/garbage_daily_report.py`：垃圾分类工作日报上下文构建。
- `scripts/generate_daily_summaries.py`：每日汇总/垃圾分类工作日报写出。
- `packaging/build_release.py`：发布目录和压缩包构建。

更细的点位指标组合判断见同目录下的 `点位指标组合判断逻辑说明.md`。

## 一、整体流程

一次完整生成流程如下：

1. 用户在 GUI 选择台账、街道日报模板、可选桶外摆日报、可选汇总模板和图片压缩等级。
2. GUI 调用 `generate_daily_reports.generate_reports(...)`。
3. 如果台账是 `.xls`，先用 Microsoft Excel COM 转为 `.xlsx`；如果已经是 `.xlsx`，直接使用。
4. 从 `.xlsx` 中读取台账字段、图片锚点、问题照片，并生成临时结构化提取文档。
5. 使用规则引擎直接从台账行构建街道日报数据对象。
6. 根据模板类型选择渲染方式：
   - 模板含 Jinja/docxtpl 标签：使用 docxtpl 渲染。
   - 模板不含 Jinja 标签：使用内置 python-docx 结构重建。
7. 每个街道生成一个街道检查日报。
8. 如果启用汇总模板，同时生成：
   - `每日汇总情况`
   - `垃圾分类工作日报`
9. 清理由主流程生成的中间 `.xlsx`、结构化提取 docx 和抽图目录。

## 二、GUI 参数设计

GUI 默认读取项目内 `input/` 和 `output/`：

- 台账默认取最新 `.xls` / `.xlsx`。
- 街道日报模板默认优先：
  - `input/daily_report_jinja_template.docx`
  - `input/街道日报模板.docx`
  - `input/日报模版.docx`
- 垃圾分类日报模板默认优先：
  - `input/垃圾分类工作日报_jinja模板.docx`
  - `input/5月17日垃圾分类工作日报.docx`
- 每日汇总模板默认优先：
  - `input/每日汇总情况_jinja模板.docx`
  - `input/每日汇总情况(10).docx`

GUI 当前提供的文件选择项：

- 台账
- 桶外摆日报
- 街道日报模板
- 垃圾分类日报模板
- 每日汇总模板

GUI 当前没有中转站 Word 上传项。虽然旧拆分脚本中有 `--transfer-doc` 参数，但主 GUI 调用 `generate_reports()` 时没有传 `transfer_doc`，当前规则生成街道日报也没有实际合并中转站 Word。

## 三、输入文件选择和转换

### 1. 台账格式

支持：

- `.xls`
- `.xlsx`

`.xlsx` 直接进入读取流程。`.xls` 会先转为 `.xlsx`。

### 2. `.xls` 转 `.xlsx`

转换只使用 Microsoft Excel COM，不使用 WPS，也没有 WPS fallback。

关键逻辑：

- 使用 `win32.DispatchEx("Excel.Application")` 启动 Excel。
- 使用 `excel.Workbooks.Open(...)` 打开 `.xls`。
- 使用 `workbook.SaveAs(..., FileFormat=51)` 保存为 `.xlsx`。
- 转换时关闭弹窗、事件、链接更新，并强制禁用宏自动化安全风险。
- 转换前会临时关闭 Excel 默认图片压缩设置，尽量保留原图。

转换输出：

- 主流程目标 `.xlsx` 位于 `output/转换xlsx/源文件名.xlsx`。
- Excel 保存过程中的临时文件位于 `D:\temp_excel_convert`。
- 项目通用临时目录位于 `tmp/`，由 `scripts/workspace_temp.py` 管理。

失败处理：

- 如果 Excel COM 无法启动，转换失败。
- 如果 Excel 有激活窗口、保护视图、文件恢复弹窗或正在编辑单元格，COM 可能拒绝调用。
- 用户可手动用 Excel 另存为 `.xlsx` 后再选择 `.xlsx` 台账。

## 四、台账字段映射

台账读取后统一映射为 `LedgerRow`：

| 台账字段 | 代码字段 | 说明 |
| --- | --- | --- |
| 2级点位 | `category` | 判断记录属于居民小区、餐饮单位、社会单位、专项检查、桶外摆等哪类。 |
| 3级点位 | `street` | 街道名称。 |
| 4级点位 | `place` | 小区/单位/点位名称。 |
| 1级指标 | `indicator1` | 部分专项、密闭式清洁站、桶外摆辅助判断使用。 |
| 2级指标 | `indicator2` | 问题归类和无问题判断的主字段。 |
| 3级指标 | `indicator3` | 经常作为最终问题文本或无问题结果。 |
| 具体问题 | `problem` | 具体问题描述、数量提取、图片段落绑定的关键字段。 |
| 创建时间 | `created_time` | 用于日期筛选。 |
| 案件上报时间 | `report_time` | 创建时间为空时可作为日期来源。 |

读取时要求存在核心列：`2级点位`、`3级点位`、`4级点位`、`1级指标`、`2级指标`、`3级指标`、`具体问题`。时间列可选。

## 五、图片抽取和放置

### 1. 图片抽取范围

程序不是把 Excel 中所有图片无条件写入报告。图片先经过两层过滤：

1. 判断哪些列是问题照片列。
2. 判断图片锚点对应哪一行。

图片列判断：

- 优先识别第 1 行中值为 `问题照片` 的单元格。
- 如果 `问题照片` 是合并单元格，则整个合并区域都作为图片列。
- 如果找不到 `问题照片` 表头，退回默认列范围 `H:Y`，即第 8 到 25 列。

桶外摆专项图片另有更窄范围：

- 只取 `H:R`，即第 8 到 18 列。

### 2. 图片绑定到台账行

`.xlsx` 中图片通过 drawing anchor 获取起始行列。程序把图片绑定到最近的数据行：

- 绑定成功后保存到 `output/转换xlsx/extracted_images/源文件名/` 等抽图目录。
- 同一行同一图片根据内容哈希和媒体名去重。
- 行对象中保存：
  - `image_paths`
  - `problem_image_paths_by_column`

`.xls` 图片处理会借助转换后的 `.xlsx` 锚点，并尽量配对 `.xls` 原始嵌入图片，以保留原图质量。

### 3. 街道日报图片放置

居民小区：

- `2级指标` 含 `小区宣传`：图片放到“小区宣传氛围”。
- `2级指标` 含 `小区公示牌`：图片放到“小区公示牌”。
- `小区内环境 + 小区内有垃圾乱堆乱放、环境脏乱现象`：图片放到“小区垃圾乱堆乱放、投放不规范现象”。
- 桶站类问题行：图片放到“桶站设置情况”。
- 居民自主投放错误行：图片放到“居民投放情况”。
- 如果仍有未分配图片，且没有桶站章节，会兜底创建 `1号桶站设置情况：无问题` 并放入剩余图片。

餐饮单位 / 社会单位：

- `2级指标 == 宣传引导`：图片放到“宣传氛围”。
- `2级指标` 或 `3级指标` 含 `容器`：图片放到“桶站设置情况”。
- 如果没有容器类行，带图片的行会作为桶站图片来源。
- 宣传行不存在时，会从该点位所有图片中稳定抽样最多 2 张作为宣传图片。
- 剩余未归类图片会合并到桶站设置图片中。

桶外摆：

- 只处理 `2级点位 == 厨余、其他垃圾桶外摆检查` 的行。
- `具体问题` 为空、`无`、`无问题` 时不写。
- 图片按两张一行排列。

### 4. 图片渲染和压缩

GUI 支持四档压缩：

- 标准
- 轻度
- 强力
- 不压缩

街道日报默认图片大小：

- 普通图片按固定宽高插入。
- 桶外摆图片按 `7.21cm x 4.06cm` 插入。

docxtpl 模板渲染时，图片对象会先转换成可插入的 InlineImage；内置渲染时用 `python-docx` 插入图片。

## 六、街道日报生成逻辑

街道日报当前只把台账中的三类 `2级点位` 写入普通正文：

- `居住小区、平房胡同`
- `餐饮单位`
- `社会单位`

以下 `2级点位` 不会进入普通街道日报正文：

- `专项检查`
- `桶外摆专项检查2`
- `中转站`
- `密闭式清洁站`
- 其他未列入三类正文的点位

桶外摆是独立章节，不属于普通正文，只认：

```text
2级点位 == 厨余、其他垃圾桶外摆检查
```

### 1. 居民小区

居民小区输出结构：

1. 小区整体情况
2. 小区宣传氛围
3. 小区公示牌
4. 纯箱房小区固定装修/大件垃圾投放点小节
5. 小区垃圾乱堆乱放、投放不规范现象
6. 桶站设置情况
7. 居民投放情况

总体问题摘要的核心优先级：

1. 特定无问题组合直接跳过，例如 `宣传引导 + 有宣传氛围`。
2. 只有 `2级指标` 属于居民自主投放类时，投放错误才统一写为 `居民自主投放不准确`；其他指标中的“不准确”按原指标问题保留，例如 `小区公示牌 + 公示牌信息不准确` 写为 `小区公示牌信息不准确`。
3. 小区内环境乱堆乱放按固定长文本写出；`小区内环境 + 散桶` 写为 `散桶`。
4. `3级指标` 有问题文本时优先使用 `3级指标`。
5. `3级指标` 是无问题但 `具体问题` 有问题关键词时，用具体问题归一化结果。
6. 其他情况使用清洗后的 `具体问题`。

大件垃圾投放点和装修垃圾投放点按父级语义单独判断：

- `大件垃圾投放点 + 有`、`装修垃圾投放点 + 有` 为无问题。
- `大件垃圾投放点 + 无` 写为 `无大件垃圾投放点`。
- `装修垃圾投放点 + 无` 写为 `无装修垃圾投放点`。
- `小区公示牌` 和 `投放点公示牌` 的问题文本会自动补齐父级前缀，例如 `公示牌破损` 会分别写成 `小区公示牌破损` 或 `投放点公示牌破损`。
- `大件垃圾投放点设置` 和 `装修垃圾投放点设置` 下的扩写 3 级指标，除 `良好，未发现问题` 外均按扩写后的指标原文写入总体问题摘要；8.13 新增的 `大件垃圾投放点无大件垃圾托底上门回收信息` 也按原文写入。
- 旧台账中的 `内容不规范（无灭火器）` 会按父级兼容转换为 `大件垃圾投放点内容不规范（灭火器材不合格）` 或 `装修垃圾投放点内容不规范（灭火器材不合格）`。

常见归一化：

- 周边 + 不洁 -> `桶站周边不洁`
- 满冒 -> `桶站满冒`
- 站外摆桶 / 桶外摆 / 垃圾桶外摆 -> `站外摆桶`
- 破损 / 脏污 -> `桶站破损、脏污`

数量：

- 默认每行 `1处`。
- `具体问题` 中写 `3处`、`三处` 等时按数量统计。
- 居民自主投放不准确通常按行计，单行有数量时按数量。

桶站设置识别重点 `2级指标`：

- `投放点环境`
- `投放点环境（容器检查）`
- `容器检查`
- `容器成组设置`
- `容器品类成组设置`
- `容器品类设置`
- `桶站便利性措施`
- `投放点公示牌`
- `早、晚高峰`
- `投放时段开盖`
- `灭蝇蚊、地面防滑设备`
- `灭蚊蝇、地面防滑设备`
- `智能回收箱是否正常运行`
- `收集车辆`

桶站编号从 `具体问题` 中提取。识别时先识别开头的编号，再判断后续状态/问题文本，因此支持 `01无问题`、`01周边不洁`、`01桶站周边不洁`、`1号桶站周边不洁`、`（1、2）桶站周边不洁`、`2-1桶站周边环境不洁` 等写法；编号会去掉前导 0，例如 `01` 输出为 `1号桶站设置情况`，`03-02A` 输出为 `3-2A号桶站设置情况`。连字符 `-` 是编号内部字符，不作为多个桶站的分隔符；多个桶站仍使用 `、`、`,`、`.`、`/`、`和` 等分隔。无编号时合并到默认桶站。

桶站设置小节的问题文本也遵循同一回退规则：如果 `3级指标` 是无问题，但 `具体问题` 中有明确问题关键词，会按 `具体问题` 归一化写入，例如 `2-1桶站周边环境不洁` 写为 `桶站周边不洁`；如果 `3级指标` 和 `具体问题` 都是无问题，则该桶站写 `无问题`。

纯箱房/纯厢房小区：

- 由点位名称或相关文本识别。
- 固定输出装修垃圾、大件垃圾投放点“预约收集，集中密闭运输。”
- 固定小节不覆盖总体问题摘要；如果台账写了 `大件垃圾投放点内容不规范（灭火器材不合格）`、`装修垃圾投放点内容不规范（灭火器材不合格）` 或旧短名称 `内容不规范（无灭火器）`，仍会在总体问题中出现。

### 2. 餐饮单位

餐饮单位按点位生成：

1. 整体情况
2. 宣传氛围
3. 桶站设置情况

问题判断采用单位类框架：

1. 命中无问题组合则跳过。
2. `3级指标` 命中该 `2级指标` 的已知子项且不是无问题时，写 `3级指标`。
3. `3级指标` 为 `有`、`是`、`无` 等泛化值时，写父级 `2级指标`。
4. `具体问题` 包含已知子项时，写该子项。
5. `2级指标` 是无子项问题项时，写 `2级指标`。
6. `2级指标` 已知但 `3级指标` 为空时，写 `2级指标`。
7. `3级指标` 有值且不是无问题时，写 `3级指标`。
8. 否则写清洗后的 `具体问题`。

餐饮单位明确无问题组合包括：

- 宣传引导情况：`有`、`有宣传氛围`
- 容器品类成组设置：`成组配置`
- 容器标识：`良好，未发现问题`
- 容器检查：`良好，未发现问题`
- 垃圾桶“游街”：`无`
- 厨余垃圾桶外摆：`无`
- 油水分离装置 / 无油水分离装置：`有` 表示无问题；`无` 表示有问题，写入文本统一为 `无油水分离装置`
- 无防蝇灭鼠设备：`有`
- 可回收物和其他垃圾乱堆乱放：`无`
- 分类投放情况：`投放正确`
- 主动提供一次性用品：整体忽略

### 3. 社会单位

社会单位结构与餐饮单位相同：

1. 整体情况
2. 宣传氛围
3. 桶站设置情况

社会单位无问题组合包括：

- 宣传引导情况：`有`、`有宣传氛围`
- 容器品类设置：`成组配置`
- 容器标识：`良好，未发现问题`
- 投放点环境（容器检查）：`良好，未发现问题`
- 垃圾桶“游街”：`无`
- 厨余垃圾桶外摆：`无`
- 可回收物和其他垃圾乱堆乱放：`无`
- 分类投放情况：`投放正确`
- 油水分离装置 / 无油水分离装置：`有` 表示无问题；`无` 表示有问题，写入文本统一为 `无油水分离装置`

数量规则同餐饮单位：默认每行 `1处`，`具体问题` 中写数量则按数量。

## 七、专项检查逻辑

`2级点位 == 专项检查` 不进入普通街道日报正文。

它用于垃圾分类工作日报中的“专项检查情况”：

- 按街道统计检查小区数。
- 按街道统计问题数。
- 目前每条问题行按 `1` 个问题计。

专项检查问题判断：

1. `具体问题` 明确含问题特征，算问题。
2. `2级指标 == 检查时段`，不算问题。
3. `2级指标 + 3级指标` 命中专项无问题表，不算问题。
4. `3级指标` 是 `良好，未发现问题`、`无问题`、`未发现问题` 等，不算问题。
5. `具体问题` 明确是 `无`、`无问题`、`未发现问题` 等，不算问题。
6. 其他情况下，只要 `3级指标` 或 `具体问题` 有内容，算问题。

专项检查问题主题会从问题内容中推断：

- 含公示、标识、便利、脚踏、大件、建设、颜色、电话等，主题偏 `桶站建设问题`。
- 含满冒、脏污、破损、周边不洁等，主题偏 `桶站满冒脏污问题`。

`1级指标` 为 `交投点` 或 `密闭式清洁站` 时，垃圾分类工作日报中会把 `4级点位` 拼到问题类型前，便于说明具体点位。旧台账中的 `清洁站` 仍兼容识别。但这不是街道日报的密闭式清洁站独立章节。

## 八、桶外摆逻辑

项目中有两套桶外摆来源：

1. 台账行：
   - 只认 `2级点位 == 厨余、其他垃圾桶外摆检查`。
   - 不再认 `2级点位 == 桶外摆专项检查2`。
2. 外部桶外摆日报 Word：
   - GUI 可选上传“桶外摆日报”。
   - 只有当台账中未识别到桶外摆记录时，才会使用外部 Word 回退，避免重复输出。

街道日报桶外摆输出：

- 以独立一级章节“桶外摆检查”写入。
- 按街道匹配。
- 每条输出 `街道：问题描述`。
- 图片按两张一行插入。

每日汇总/垃圾分类工作日报中也会根据桶外摆信息生成街道级 `outside_bucket_summary`。

## 九、垃圾分类工作日报逻辑

垃圾分类工作日报上下文由 `build_garbage_daily_context()` 构建，包含：

- 报告日期
- 街道数
- 市级检查默认说明
- 居民小区街道汇总
- 社会单位街道汇总
- 餐饮单位街道汇总
- 专项检查情况
- 城管执法默认数据
- 生活垃圾清运量默认数据

它的居民/单位问题归类不是直接复用街道日报文本，而是用轻量关键词映射。

居民小区汇总识别：

- `桶站满冒`
- `桶站周边不洁`
- `高峰时段桶站未开盖`
- `居民自主投放不准确`
- `站外摆桶`
- `无宣传氛围`
- `无小区公示牌`
- `无桶站`
- `散桶`
- `垃圾车混装混运`

社会单位/餐饮单位汇总识别：

- `分类投放不准确`
- `桶站周边不洁`
- `桶站满冒`
- `站外摆桶`
- `无宣传氛围`

同一点位下相同问题会累计数量，输出类似 `桶站满冒2处`。

## 十、每日汇总逻辑

每日汇总有两种上下文来源：

1. 优先用 `ledger_rows` 直接构建垃圾日报上下文。
2. 如果没有 `ledger_rows`，则从已生成的街道日报文本中反向抽取问题。

当前主流程会传入 `ledger_rows` 和街道日报路径，因此每日汇总主要基于台账构建上下文，并补充街道问题计数。

街道问题计数规则：

- 遍历居民小区街道下各点位的 `issues`。
- 若 issue 中有整数 `count`，直接用。
- 若文本中含 `3处`、`三处`、`十一处`、`二十处` 等，按数量。
- 没有明确数量时按 `1`。

## 十一、模板渲染设计

街道日报支持两种渲染方式：

### 1. docxtpl/Jinja 模板

如果模板内含 Jinja 标签，使用 docxtpl：

- 保留模板页边距、样式、段落、图片占位结构。
- 图片会转成 InlineImage。
- 模板控制段落显示时使用 `{%p if ... %}` / `{%p for ... %}`。
- 同一段内控制文本使用普通 `{% if ... %}`。
- 小区整体情况段如果只写 `存在的问题是：{{ community.overall_problem_summary }}`，且没有写 `{{ community.overall_intro }}`，渲染前会自动在同一段前面补入 `{{ community.overall_intro }}`；模板已显式写入时不会重复补入。

主要模板变量：

- `communities`
- `restaurants`
- `social_units`
- `outside_bucket_issues`
- `report_title`
- `report_date_text`

### 2. 内置 python-docx 渲染

模板不含 Jinja 标签时，程序会新建文档并按内置结构写入：

- 居住小区
- 餐饮单位
- 社会单位
- 桶外摆检查

这种方式不会完整保留用户模板的复杂格式。

## 十二、输出目录和中间文件

默认目录：

- `output/转换xlsx/`：转换后的 `.xlsx`。
- `output/提取docx/`：结构化提取 Word。
- `output/`：街道日报输出。
- `output/每日总结/每日总结（日期）/`：每日汇总和垃圾分类工作日报。
- `tmp/`：项目内临时目录。

主流程生成完成后会清理：

- 本次生成的中间 `.xlsx`。
- 本次生成的结构化提取 docx。
- 本次生成的抽图临时目录。

不会清理：

- 用户放入 `input/` 的台账和模板。
- 已生成的最终街道日报。
- 已生成的每日总结。
- release 目录中的发布文件。

## 十三、发布打包逻辑

默认发布入口：

```powershell
python packaging\build_release.py
```

构建过程：

1. 设置 PyInstaller 临时目录到项目内 `tmp/pyinstaller`。
2. 运行 `PyInstaller --clean --noconfirm packaging/daily_report_gui.spec`。
3. 将 `dist/日报生成工具` 同步到 `release/日报生成工具`。
4. 创建 `input` 和 `output` 子目录。
5. 复制默认模板、README、使用手册和启动脚本。
6. 默认创建 `release/日报生成工具.zip`。

发布目录必须整体迁移，不应只复制 exe，因为 exe 依赖 `_internal` 和配套资源。

如果用户明确说“不用打包 zip”，只生成/同步 `release/日报生成工具`，不创建压缩包，并删除旧 zip 避免混淆。

## 十四、当前已知边界

1. GUI 没有中转站 Word 上传项。
2. 台账中 `2级点位 == 中转站` 或 `密闭式清洁站` 不会进入街道日报普通正文；旧台账 `清洁站` 仍按同类兼容处理。
3. `专项检查` 不进入普通街道日报，只进入垃圾分类工作日报专项检查表。
4. `桶外摆专项检查2` 当前不会进入街道日报桶外摆章节。
5. `.xls` 转换依赖 Microsoft Excel COM，不使用 WPS。
6. Excel 弹窗、保护视图、激活窗口会阻塞自动转换。
7. 图片不是全部插入，而是按问题照片列、行锚点和报告段落规则筛选后插入。
8. 街道日报和垃圾分类工作日报的问题归类不是完全同一套规则，后者使用更轻量的关键词汇总。

## 十五、维护要求

修改代码逻辑时，需要同步检查：

- `README.md`
- `使用手册.txt`
- `点位指标组合判断逻辑说明.md`
- `程序逻辑细则.md`

如果修改了模板变量、输出结构、点位判断、图片规则、打包行为或 GUI 参数，需要更新对应说明，避免交付文档和实际程序不一致。

模板文件未经明确允许不要修改。临时文件应写入项目内 `tmp/` 或其他非 C 盘路径。
