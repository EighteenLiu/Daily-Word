# 点位指标组合判断逻辑说明

本文档按当前代码逻辑整理，主要对应以下文件：

- `scripts/daily_report_builder.py`：生成各街道检查日报。
- `scripts/garbage_daily_report.py`：生成全区垃圾分类工作日报和每日汇总。

台账字段在代码中的对应关系如下：

| 台账字段 | 代码字段 | 主要用途 |
| --- | --- | --- |
| 2级点位 | `category` | 决定记录属于居民小区、餐饮单位、社会单位、专项检查、桶外摆检查等哪一类。 |
| 3级点位 | `street` | 街道名称，用于筛选和分街道汇总。 |
| 4级点位 | `place` | 点位名称，用于同一街道下分点位/小区/单位。 |
| 1级指标 | `indicator1` | 部分专项、桶外摆、辅助分类逻辑会参考。 |
| 2级指标 | `indicator2` | 判断问题类型和是否为无问题的重要字段。 |
| 3级指标 | `indicator3` | 多数情况下优先作为最终问题名称或无问题结果。 |
| 具体问题 | `problem` | 判断是否真实有问题、计数、补充问题文本的关键字段。 |

## 一、街道检查日报的点位入口

街道检查日报只把 `2级点位` 分成三类正文：

- `居住小区、平房胡同`
- `餐饮单位`
- `社会单位`

`2级点位` 不是以上三类的记录，不会进入普通正文。

另有一个独立的“桶外摆检查”章节。当前逻辑只认：

```text
2级点位 == 厨余、其他垃圾桶外摆检查
```

也就是说，`2级点位 == 桶外摆专项检查2` 不会再因为 `1级指标 == 厨余、其他垃圾桶外摆检查` 被写进街道日报。

## 二、通用无问题判断

在街道日报里，以下文本通常会被当作无问题：

- 空值
- `无问题`
- `良好，未发现问题`
- `良好,未发现问题`
- `未发现问题`
- `有宣传氛围`
- `均有设置`
- `设施齐全`
- `已核实`
- `投放正确`
- `正常运行`
- 单纯数字或容器数量描述，例如“本小区检查了5个容器”

但如果文本里同时包含问题关键词，就不会简单当作无问题。问题关键词包括：

- `不洁`
- `不准确`
- `站外摆桶`
- `桶外摆`
- `外摆`
- `满冒`
- `破损`
- `脏污`
- `不齐全`
- `无宣传`
- `未开盖`
- `混投`
- `散桶`

## 三、居民小区、平房胡同

### 1. 总体“存在的问题是”如何判断

居民小区总体问题摘要会遍历同一小区的台账行。以下情况会跳过：

- `2级指标 == 检查时段`
- 不是居民自主投放错误，并且 `具体问题` 是无问题，同时 `3级指标` 也是无问题
- 不是桶站类问题指标，并且 `具体问题` 是明确无问题

进入问题判断后，最终问题名称按以下优先级生成：

1. 如果 `2级指标` 是 `宣传引导`，且 `3级指标 == 有宣传氛围`，视为无问题。
2. 如果 `2级指标` 是 `小区公示牌`，且 `3级指标 == 良好，未发现问题`，视为无问题。
3. 如果 `2级指标` 是居民自主投放类，且 `具体问题` 或 `3级指标` 包含 `投放错误` 或 `不准确`，统一写为 `居民自主投放不准确`；其他指标里的“不准确”（例如 `小区公示牌 + 公示牌信息不准确`）按该指标自身问题写出。
4. 如果 `2级指标 == 小区内环境` 且 `3级指标 == 小区内有垃圾乱堆乱放、环境脏乱现象`，并且 `具体问题` 不是“无/未发现/无乱堆乱放”等否定描述，写为 `小区内有垃圾乱堆乱放、环境脏乱现象`。
5. 如果 `3级指标` 有值且不是无问题，优先把 `3级指标` 作为问题名称。
6. 如果 `3级指标` 是无问题，但 `具体问题` 里有问题关键词，则按关键词规范化，例如：
   - 含 `周边` 和 `不洁` -> `桶站周边不洁`
   - 含 `满冒` -> `桶站满冒`
   - 含 `站外摆桶`、`桶外摆`、`垃圾桶外摆` -> `站外摆桶`
   - 含 `破损` 或 `脏污` -> `桶站破损、脏污`
7. 其他情况使用清洗后的 `具体问题`。

例子：

| 2级指标 | 3级指标 | 具体问题 | 结果 |
| --- | --- | --- | --- |
| 宣传引导 | 有宣传氛围 | 无 | 不写问题 |
| 小区公示牌 | 良好，未发现问题 | 无 | 不写问题 |
| 小区公示牌 | 公示牌信息不准确 | 公示牌信息不准确 | 小区公示牌信息不准确 |
| 居民自主投放情况 | 投放错误 | 无 | 居民自主投放不准确 |
| 小区内环境 | 小区内有垃圾乱堆乱放、环境脏乱现象 | 现场有杂物堆放 | 小区内有垃圾乱堆乱放、环境脏乱现象 |
| 投放点环境 | 良好，未发现问题 | 桶站周边不洁 | 桶站周边不洁 |
| 大件垃圾投放点 | 无 | 无 | 无大件垃圾投放点 |
| 装修垃圾投放点 | 无 | 无 | 无装修垃圾投放点 |
| 大件垃圾投放点设置 | 大件垃圾投放点内容不规范（灭火器材不合格） | 现场缺少设备 | 大件垃圾投放点内容不规范（灭火器材不合格） |

大件垃圾投放点和装修垃圾投放点有父级语义：`有` 表示无问题，`无` 表示问题，不能套用全局 `无` 等于无问题的规则。

`大件垃圾投放点设置` 的 3 级指标已扩写为带父级前缀的完整名称。`良好，未发现问题` 不写问题；以下 3 级指标按原名称写入总体问题摘要：

- `大件垃圾投放点公示牌信息错误`
- `大件垃圾投放点无大件垃圾托底上门回收信息`
- `大件垃圾投放点未设置公示牌`
- `大件垃圾投放点未设置围挡或专门隔离区`
- `大件垃圾投放点地面未作硬化处理`
- `大件垃圾投放点附近道路运输车通行不便`
- `大件垃圾投放点大件垃圾未有序码放`
- `大件垃圾投放点周围存在环境卫生死角`
- `大件垃圾投放点小于6平米`
- `大件垃圾投放点内容不规范（灭火器材不合格）`
- `大件垃圾投放点大件与装修或生活垃圾混放`

`装修垃圾投放点设置` 的 3 级指标同样已扩写。`良好，未发现问题` 不写问题；以下 3 级指标按原名称写入总体问题摘要：

- `装修垃圾投放点公示牌信息错误`
- `装修垃圾投放点未设置公示牌`
- `装修垃圾投放点未设置围挡或专门隔离区`
- `装修垃圾投放点地面未作硬化处理`
- `装修垃圾投放点附近道路运输车通行不便`
- `装修垃圾投放点未袋装并有序码放`
- `装修垃圾投放点周边存在环境卫生死角`
- `装修垃圾投放点小于6平米`
- `装修垃圾投放点内容不规范（灭火器材不合格）`
- `装修垃圾投放点装修与大件或生活垃圾混放`
- `无装修垃圾备案`

为兼容旧台账，旧短名称 `内容不规范（无灭火器）` 会按父级转换为对应的扩写名称 `大件垃圾投放点内容不规范（灭火器材不合格）` 或 `装修垃圾投放点内容不规范（灭火器材不合格）`。

### 2. 居民小区问题数量怎么计算

居民小区总体问题按问题名称合并计数。

- 普通问题默认每行算 `1处`。
- 如果 `具体问题` 中写了 `3处`、`三处` 等，会按对应数量统计。
- `居民自主投放不准确` 通常按行数统计；如果只有一行且 `具体问题` 写了数量，则按数量。
- 特殊规则：`西绒线13号院` 且问题为 `其他投厨余` 时，居民自主投放不准确按 `2` 计。

### 3. 桶站设置情况怎么判断

居民小区的桶站设置不是所有问题都写进去，只重点识别桶站类 `2级指标`：

- `投放点环境`
- `容器成组设置`
- `桶站便利性措施`
- `投放点公示牌`
- `早、晚高峰`
- `投放时段开盖`
- `灭蝇蚊、地面防滑设备`
- `灭蚊蝇、地面防滑设备`
- `智能回收箱是否正常运行`
- `收集车辆`

桶站问题文本生成规则：

1. 如果 `具体问题` 和 `3级指标` 都是无问题，不写。
2. 如果 `3级指标` 是无问题，但 `具体问题` 里有问题关键词，则按 `具体问题` 归一化生成，例如 `2-1桶站周边环境不洁` 写为 `桶站周边不洁`。
3. 如果 `3级指标` 是无问题，且 `具体问题` 也没有真实问题关键词，不写。
4. 如果 `3级指标` 有问题文本，优先写 `3级指标`。
5. 否则写清洗后的 `具体问题`。

桶站编号优先从 `具体问题` 中识别。识别时先识别开头的编号，再判断后续状态/问题文本，因此 `01无问题`、`01周边不洁`、`01桶站周边不洁`、`1号桶站周边不洁`、`（1、2）桶站周边不洁`、`2-1桶站周边环境不洁` 都能识别；编号会去掉前导 0，例如 `01` 输出为 `1号桶站设置情况`，`03-02A` 输出为 `3-2A号桶站设置情况`。连字符 `-` 是编号内部字符，不作为多个桶站的分隔符；多个桶站仍使用 `、`、`,`、`.`、`/`、`和` 等分隔。如果没有明确编号，会合并到默认的 `1号桶站设置情况`。

### 4. 纯箱房小区固定小节

如果 `4级点位` 或相关文本包含 `纯箱房小区`、`纯厢房小区`，会固定输出：

- `装修垃圾投放点设置：预约收集，集中密闭运输。`
- `大件垃圾投放点设置：预约收集，集中密闭运输。`

这两个固定小节不代表对应台账行一定是无问题。若台账中 `大件垃圾投放点设置` 的 `3级指标` 是 `大件垃圾投放点内容不规范（灭火器材不合格）`，或者旧台账仍写 `内容不规范（无灭火器）`，它仍会出现在前面的总体问题摘要中。

## 四、餐饮单位

餐饮单位和社会单位共用一套“单位类”判断框架，但指标表不同。

餐饮单位明确支持的 `2级指标 -> 3级指标` 组合包括：

| 2级指标 | 识别的3级指标 |
| --- | --- |
| 宣传引导情况 | 无垃圾分类投放指引、无四周年宣传海报、无宣传氛围、无源头减量宣传（光盘行动） |
| 容器品类成组设置 | 后厨未成组设置垃圾桶、无容器设置、成组配置、集中用餐区未成组设置容器 |
| 容器标识 | 良好，未发现问题、容器标识不正确或无标识、容器标识不清晰、破损、容器颜色不正确 |
| 容器检查 | 容器垃圾不纯净、良好，未发现问题、投放点外表面明显脏污、桶站破损、脏污、桶站周边不洁、桶站满冒、站外摆桶 |
| 垃圾收运合同 | 无厨余垃圾收运合同或不合格、无其他垃圾收运合同或不合格 |
| 垃圾排放登记 | 无厨余垃圾排放登记方式、无非居民其他垃圾排放登记方式 |
| 垃圾桶“游街” | 有、无 |
| 厨余垃圾桶外摆 | 有、无 |
| 油水分离装置 / 无油水分离装置 | 有、无；`有` 表示无问题，`无` 表示有问题且写为“无油水分离装置” |
| 可回收物和其他垃圾乱堆乱放 | 有、无 |
| 主动提供一次性用品 | 是、否 |
| 分类投放情况 | 投放正确、投放错误 |
| 无防蝇灭鼠设备 | 有 |

餐饮单位明确视为无问题的组合：

| 2级指标 | 无问题3级指标或具体问题 |
| --- | --- |
| 宣传引导情况 | 有、有宣传氛围；具体问题含“有宣传氛围” |
| 容器品类成组设置 | 成组配置 |
| 容器标识 | 良好，未发现问题 |
| 容器检查 | 良好，未发现问题 |
| 垃圾桶“游街” | 无 |
| 厨余垃圾桶外摆 | 无 |
| 油水分离装置 / 无油水分离装置 | 有；具体问题含“有油水分离装置” |
| 无防蝇灭鼠设备 | 有；具体问题含“有防蝇灭鼠设备”等 |
| 可回收物和其他垃圾乱堆乱放 | 无 |
| 主动提供一次性用品 | 否；且该指标整体被忽略，不写问题 |
| 分类投放情况 | 投放正确 |

单位类最终问题名称优先级：

1. 如果命中无问题组合，不写。
2. 如果 `3级指标` 在该 `2级指标` 的已知子项里，且不是无问题，则写 `3级指标`。
3. 如果 `3级指标` 是 `有`、`是`、`无` 这类泛化结果，则写父级 `2级指标`。
4. 如果 `具体问题` 里包含某个已知子项，写该子项。
5. 如果 `2级指标` 本身是一个无子项的问题项，写 `2级指标`。
6. 如果 `2级指标` 已知但 `3级指标` 为空，写 `2级指标`。
7. 如果 `3级指标` 有值且不是无问题，写 `3级指标`。
8. 否则写清洗后的 `具体问题`。

数量规则：

- 默认每行 `1处`。
- 如果 `具体问题` 写了 `3处`、`3个`、`3组`、`三处` 等，按对应数量。

## 五、社会单位

社会单位支持的 `2级指标 -> 3级指标` 组合包括：

| 2级指标 | 识别的3级指标 |
| --- | --- |
| 宣传引导情况 | 有、无、无宣传氛围、无源头减量宣传（光盘行动）、无四周年宣传海报 |
| 容器品类设置 | 单位无容器配置、公共场所区域未成组设置可回收物和其他容器、集中用餐区未成组设置厨余和其他容器、食品加工区未成组设置厨余和其他容器、成组配置、未成组配置 |
| 容器标识 | 良好，未发现问题、容器标识不正确、容器标识不清晰、破损、容器颜色不正确 |
| 投放点环境（容器检查） | 良好，未发现问题、投放点外表面明显脏污、桶站破损、脏污、桶站周边不洁、桶站满冒、站外摆桶 |
| 垃圾桶“游街” | 有、无 |
| 厨余垃圾桶外摆 | 有、无 |
| 可回收物和其他垃圾乱堆乱放 | 有、无 |
| 分类投放情况 | 投放正确、投放错误 |
| 收运合同 | 无厨余垃圾收运合同或不合格、无其他垃圾收运合同或不合格、无可回收物收运合同或不合格、无有害垃圾收运合同或不合格 |
| 排放登记 | 无厨余垃圾排放登记方式、无非居民其他垃圾排放登记方式 |

社会单位明确视为无问题的组合：

| 2级指标 | 无问题3级指标或具体问题 |
| --- | --- |
| 宣传引导情况 | 有、有宣传氛围；具体问题含“有宣传氛围” |
| 容器品类设置 | 成组配置 |
| 容器标识 | 良好，未发现问题 |
| 投放点环境（容器检查） | 良好，未发现问题 |
| 垃圾桶“游街” | 无 |
| 厨余垃圾桶外摆 | 无 |
| 可回收物和其他垃圾乱堆乱放 | 无 |
| 分类投放情况 | 投放正确 |
| 油水分离装置 / 无油水分离装置 | 有；具体问题含“有油水分离装置” |

社会单位的问题名称和数量规则与餐饮单位相同。

## 六、桶外摆检查

街道日报里的“桶外摆检查”独立于普通三类点位。当前只从台账中提取：

```text
2级点位 == 厨余、其他垃圾桶外摆检查
```

进入后还会继续判断：

- `3级点位` 必须能匹配当前街道。
- `具体问题` 不能为空。
- `具体问题` 不能是 `无`、`无问题`。
- 输出时会去掉开头的街道名，并补句号。
- 图片只取问题照片列范围内的图片。

不再提取：

```text
2级点位 == 桶外摆专项检查2
```

即使它的 `1级指标 == 厨余、其他垃圾桶外摆检查`，也不会进入街道日报桶外摆检查章节。

## 七、专项检查

`2级点位 == 专项检查` 不写入普通街道检查日报正文。它用于全区垃圾分类工作日报中的“专项检查情况”章节。

专项检查按街道汇总：

- `检查小区数`：同一街道下不同 `4级点位` 的数量。
- `问题数`：命中问题判断的行数，目前每行按 `1` 计。

专项检查的有问题判断：

1. 如果 `具体问题` 明确含问题词，直接算有问题。
2. 如果 `2级指标 == 检查时段`，不算问题。
3. 如果 `2级指标 + 3级指标` 命中专项检查无问题表，不算问题。
4. 如果 `3级指标` 是 `良好，未发现问题`、`无问题`、`未发现问题` 等，不算问题。
5. 如果 `具体问题` 明确是 `无`、`无问题`、`未发现问题` 等，不算问题。
6. 其余情况下，只要 `3级指标` 或 `具体问题` 有内容，就算有问题。

专项检查无问题组合包括：

| 2级指标 | 无问题3级指标 |
| --- | --- |
| 智能回收箱是否正常运行 | 正常运行 |
| 宣传引导 | 有宣传氛围 |
| 居民知晓垃圾分类情况 | 合格 |
| 小区公示牌 | 良好，未发现问题 |
| 小区内环境 | 良好，未发现问题 |
| 投放点宣传指引 | 有宣传氛围 |
| 投放点公示牌设置 | 有 |
| 投放点公示牌 | 良好，未发现问题 |
| 容器成组设置 | 良好，未发现问题 |
| 容器标识 | 良好，未发现问题 |
| 遮雨棚 | 有 |
| 投放点环境 | 良好，未发现问题 |
| 桶站便利性措施 | 有，能正常使用 |
| 早、晚高峰投放时段开盖 | 符合要求 |
| 灭蚊蝇、地面防滑设备 | 均有设置 |
| 保洁人员作业规范 | 良好，未发现问题 |
| 收集车辆 | 良好，未发现问题 |
| 可回收物体系 | 良好，未发现问题 |
| 大件垃圾投放点 | 有 |
| 大件垃圾投放点设置 | 良好，未发现问题 |
| 装修垃圾投放点 | 有 |
| 装修垃圾投放点设置 | 良好，未发现问题 |
| 居民自主投放情况 | 投放正确 |

专项检查中，`具体问题` 只要含以下问题特征之一，通常会判为有问题：

- `不`
- `无`
- `未`
- `遮挡`
- `脏污`
- `破损`
- `错误`
- `不符`
- `不齐全`
- `不能`
- `无法`
- `反转`
- `打不开`
- `缺`
- `混投`
- `敞口`
- `遗撒`
- `滴漏`
- `非京牌`
- `外摆`
- `满冒`

## 八、全区垃圾分类工作日报中的问题汇总

全区垃圾分类工作日报会把居民小区、社会单位、餐饮单位按街道和点位汇总。它使用的是另一套轻量化问题归类规则，不完全等同于街道检查日报正文。

居民小区汇总识别的问题类型：

- `桶站满冒`
- `桶站周边不洁`
- `高峰时段桶站未开盖`
- `居民自主投放不准确`
- `站外摆桶`
- `无宣传氛围`
- `无小区公示牌`
- `无桶站`
- `散桶`
- `垃圾车混装混运`

社会单位、餐饮单位汇总识别的问题类型：

- `分类投放不准确`
- `桶站周边不洁`
- `桶站满冒`
- `站外摆桶`
- `无宣传氛围`

归类时会综合清洗后的 `3级指标`、`具体问题` 等文本，按关键词匹配。每个问题类型会累计数量，并输出为类似 `桶站满冒2处` 的形式。

## 九、常见边界情况

### 1. `3级指标` 和 `具体问题` 谁优先？

- 居民小区总体问题：多数情况下 `3级指标` 优先；但如果 `3级指标` 是无问题，而 `具体问题` 有明确问题关键词，则用 `具体问题` 归一化结果。
- 居民小区桶站设置：`3级指标` 有问题时优先；如果 `3级指标` 是无问题但 `具体问题` 有明确问题关键词，则用 `具体问题` 归一化结果；否则再按无问题或清洗后的 `具体问题` 处理。
- 餐饮单位、社会单位：先看 `2级指标 + 3级指标` 是否命中已知组合；未命中时再看 `具体问题`。

### 2. `具体问题` 写“xx问题3处”怎么算？

按 `3处` 计。支持阿拉伯数字和部分中文数字。

### 3. `3级指标` 是“良好，未发现问题”，但 `具体问题` 写了问题，算不算？

居民小区中，如果 `具体问题` 含问题关键词，会算问题，并按关键词规范化。

餐饮单位、社会单位中，如果 `3级指标` 已命中该指标的无问题结果，通常直接视为无问题；只有未命中无问题组合时，才继续看 `具体问题`。

### 4. `2级点位 == 专项检查` 会不会进街道日报？

不会进入普通街道检查日报正文。它只进入全区垃圾分类工作日报的“专项检查情况”。

### 5. `2级点位 == 桶外摆专项检查2` 会不会进街道日报？

当前不会。街道日报桶外摆章节只认 `2级点位 == 厨余、其他垃圾桶外摆检查`。

